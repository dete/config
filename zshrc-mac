# xcrun aliases

TOOLS=(git svn otool)

for tool in $TOOLS; do
   if ! which $tool >&/dev/null && which xcrun >&/dev/null; then
      alias $tool="xcrun $tool"
   fi
done

alias ff='open -a Xee'
alias mp="mplayer"
